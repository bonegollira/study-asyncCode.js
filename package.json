{
  "name": "study-asyncCode.js",
  "version": "0.0.0",
  "description": "JavaScriptで非同期で処理を走らせるやり方",
  "scripts": {
    "start": "gulp",
    "deploy": "gulp ghPages"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:bonegollira/study-asyncCode.js.git"
  },
  "dependencies": {
  },
  "devDependencies": {
    "browser-sync": "*",
    "gulp": "*",
    "gulp-gh-pages": "*"
  }
}
